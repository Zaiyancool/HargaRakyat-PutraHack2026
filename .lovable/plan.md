# plan

News section should be on their own page
The top navigation bar on landing page should be standardize across the system, and use hamburger button in mobile view.
The system should be mobile friendly responsive across different device
Show the top 100 item
Below the top button, should have next 200 item using >> or << symbol like the kraken
Price map and store finder page i think we can combine the functionality
Smart basket should have the functionality to link with e commerce like shopee, lotus, foodpanda, grab and so on so user can shopping online
each top navbar should link with respective page